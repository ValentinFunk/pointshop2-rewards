box: node:7

build:
  steps:
    - npm-install

deploy:
  steps:
    - npm-install
    - script:
      name: generate dist
      code: |
        npm run build
    - script:
      name: update version
      code: |
        npm run sf-release
